import * as React from 'react';

// Per Gemini API guidelines, API key management in the UI is not allowed.
// The key must come from process.env.API_KEY.
// This component is intentionally left blank to adhere to the project requirements.
export const ApiKeyDialog: React.FC = () => {
  return null;
};